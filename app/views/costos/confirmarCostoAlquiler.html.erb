<h1 class="text-center mt-3 mb-4">Confirmación</h1>
<p class="text-center mb-4">¿Está seguro de cambiar la tarifa de $<%= @viejo_costo %> la hora a $<%= @nuevo_costo %> la hora?</p>

<div class="d-flex justify-content-center">
    <%= form_tag cambiar_costo_alquiler_path, method: "get" do %>
        <%= hidden_field_tag :monto, @nuevo_costo %>
        <div class="mb-3 d-flex justify-content-center">
            <%= link_to "Cancelar", costo_alquiler_path ,class: "btn btn-danger mx-2" %>  
            <%= submit_tag "Confirmar", class: "btn btn-success mx-2" %>
        </div>
    <% end %>
</div>