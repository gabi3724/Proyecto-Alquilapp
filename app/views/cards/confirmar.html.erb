<h1 class="text-center mt-3 mb-5">Detalles de la operación</h1>
<div class="d-flex justify-content-center mb-4">
    <div class="col col-md-3">
        <h5 class="row mb-3">Saldo a cargar</h5>
        <h5 class="row mb-3">Saldo antes de la carga</h5>
        <h5 class="row mb-3">Saldo luego de la carga</h5>
    </div>
    <div class="col col-md-1">
        <div class="row">
            <p class="text-center"> $<%= @monto %> </p>
        </div>
        <div class="row">
            <p class="text-center"> $<%= @usuario.creditos %> </p>
        </div>
        <div class="row">
            <p class="text-center"> $<%= @usuario.creditos+@monto %> </p>
        </div>
    </div>
</div>
<div class="d-flex justify-content-center">
    <div class="row">
            <div class="col">
                <%= link_to "Cancelar", cargar_saldo_path, class: "btn btn-danger" %>
            </div>
            <div class="col">
                <%= form_tag carga_path, method: "get" do %>
                    <%= hidden_field_tag :monto, @monto %>
                    <%= submit_tag "Confirmar", class: "btn btn-success" %>
                <% end %>
            </div>
    </div>
</div>