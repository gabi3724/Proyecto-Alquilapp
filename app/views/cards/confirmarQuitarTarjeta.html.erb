<h1 class="text-center mt-3">Confirmación</h1>

<div class="d-flex justify-content-center mb-4">
    <div class="col col-md-5">
        <h5 class="mb-5 text-center">Está por quitar esta tarjeta</h5>
        <% numero= @card.numero %>
        <% for i in 0..numero.size-5 %>
            <% numero[i]="X" %>
        <% end %>
        <div class="row d-flex justify-content-center mb-4">
            <div class="col col-md-5">
                <h5 class="row mb-3"> Número: </h5>
                <h5 class="row mb-3"> Fecha de vencimiento: </h5>
            </div>
            <div class="col col-md-4">
                <div class="row"> <p class="text-center"> <%= numero %> </p> </div>
                <div class="row"> <p class="text-center"> <%= @card.fecha_vencimiento.to_time.strftime("%m/%Y") %> </p> </div>
            </div>
        </div>
        <h5 class="text-center">¿Desea hacerlo?</h5>
    </div>
</div>
<div class="d-flex justify-content-center">
    <div class="row">
        <div class="col">
            <%= link_to "Cancelar", usuario_path(@card.usuario_id), class: "btn btn-primary-orange" %>
        </div>
        <div class="col">
            <%= button_to "Quitar", card_path(@card), :method => :delete, class: "btn btn-danger" %>
        </div>
    </div>
</div>